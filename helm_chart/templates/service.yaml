apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.detailsApp.name }}
spec:
  selector:
    app: {{ .Values.detailsApp.name }}
  ports:
    - name: http
      port: {{ .Values.detailsApp.port }}
      targetPort: {{ .Values.detailsApp.port }}
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.nginx.name }}
spec:
  selector:
    app: {{ .Values.nginx.name }}
  ports:
    - name: http
      port: {{ .Values.nginx.port }}
      targetPort: {{ .Values.nginx.port }}
  type: NodePort

